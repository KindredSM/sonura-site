---
import BaseLayout from './BaseLayout.astro';

interface WorkflowItem {
  /* asset base path, e.g. '/assets/workflow-design-final' (needs -768/-1280 avif+webp variants) */
  image: string;
  imageAlt: string;
  title: string;
  description: string;
}

interface FAQ {
  question: string;
  answer: string;
}

interface DemoTrack {
  title: string;
  tags: string[];
  useCase: string;
  audioSrc: string;
}

interface Props {
  genre: string;
  genreSlug: string;
  title: string;
  description: string;
  keywords: string;
  headline: string;
  subhead: string;
  ctaText: string;
  workflowTitle: string;
  workflowItems: WorkflowItem[];
  faqs: FAQ[];
  proseTitle?: string;
  proseBody?: string;
  demoTitle?: string;
  demoTracks?: DemoTrack[];
  trustItems?: string[];
  finalCtaTitle?: string;
  finalCtaButtonText?: string;
  appUrl?: string;
}

const {
  genre,
  genreSlug,
  title,
  description,
  keywords,
  headline,
  subhead,
  ctaText,
  workflowTitle,
  workflowItems,
  faqs,
  proseTitle,
  proseBody,
  demoTitle,
  demoTracks = [],
  trustItems = [],
  finalCtaTitle,
  finalCtaButtonText,
  appUrl = 'https://app.sonurastudio.com/',
} = Astro.props;

const canonical = `https://sonurastudio.com/genre/${genreSlug}/`;
const stripHtml = (value: string) => value.replace(/<[^>]*>/g, '').replace(/\s+/g, ' ').trim();

/* Numbered section kickers. Prose and demo sections are optional, so numbering
   is computed from whichever sections are actually present. */
const hasProse = Boolean(proseTitle && proseBody);
const hasDemo = demoTracks.length > 0;
let sectionNo = 0;
const nextKicker = (label: string) => `${String(++sectionNo).padStart(2, '0')} / ${label}`;
const kickers = {
  workflow: nextKicker('Workflow'),
  prose: hasProse ? nextKicker('Why Sonura') : '',
  demo: hasDemo ? nextKicker('Demos') : '',
  next: nextKicker('Next steps'),
  faq: nextKicker('FAQ'),
};

const jsonLd = [
  {
    "@context": "https://schema.org",
    "@type": "WebApplication",
    "name": `Sonura ${genre} Beat Maker`,
    "url": canonical,
    "applicationCategory": "MultimediaApplication",
    "genre": genre,
    "description": description
  },
  {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    "itemListElement": [
      { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://sonurastudio.com/" },
      { "@type": "ListItem", "position": 2, "name": `${genre} Beat Maker`, "item": canonical }
    ]
  },
  {
    "@context": "https://schema.org",
    "@type": "FAQPage",
    "mainEntity": faqs.map(faq => ({
      "@type": "Question",
      "name": faq.question,
      "acceptedAnswer": {
        "@type": "Answer",
        "text": stripHtml(faq.answer)
      }
    }))
  }
];
---

<BaseLayout
  title={title}
  description={description}
  keywords={keywords}
  canonical={canonical}
  ogTitle={title}
  ogDescription={description}
  jsonLd={jsonLd}
  bodyClass="genre-page"
>
  <style>
    .hero-beta-link {
      align-self: flex-start;
    }
    .genre-prose {
      border-top: 1px solid var(--hairline);
      padding: clamp(72px, 9vw, 110px) var(--page-pad);
    }
    .genre-prose-inner {
      max-width: 820px;
      margin: 0;
    }
    .genre-prose-title {
      margin: 0 0 28px;
      font-family: var(--font-display);
      font-weight: 400;
      font-size: clamp(32px, 4.2vw, 56px);
      letter-spacing: 0.01em;
      line-height: 1.02;
      text-transform: uppercase;
      color: var(--fg);
    }
    .genre-prose-body :global(p) {
      margin: 0 0 20px;
      color: var(--muted);
      font-size: clamp(16px, 1.4vw, 18px);
      line-height: 1.75;
    }
    .genre-prose-body :global(p:last-child) {
      margin-bottom: 0;
    }
    .genre-prose-body :global(strong) {
      color: var(--fg);
      font-weight: 600;
    }
    .genre-prose-body :global(a) {
      color: var(--fg);
      text-decoration: underline;
      text-underline-offset: 3px;
    }
    .genre-next {
      border-top: 1px solid var(--hairline);
      padding: clamp(72px, 9vw, 110px) var(--page-pad);
    }
    .genre-next-inner {
      max-width: none;
      margin: 0;
    }
    .genre-next-title {
      margin: 0 0 48px;
      text-align: left;
      font-family: var(--font-display);
      font-weight: 400;
      font-size: clamp(38px, 4.8vw, 68px);
      letter-spacing: 0.01em;
      line-height: 1;
      text-transform: uppercase;
      color: var(--fg);
    }
    .genre-next-grid {
      display: block;
      counter-reset: genre-next;
      border-bottom: 1px solid var(--hairline);
    }
    .genre-next-link {
      counter-increment: genre-next;
      display: grid;
      grid-template-columns: 64px minmax(0, 300px) 1fr;
      gap: 24px;
      align-items: baseline;
      padding: 22px 0;
      border-top: 1px solid var(--hairline);
      border-radius: var(--radius-ui);
      color: var(--fg);
      text-decoration: none;
      transition: color 0.2s;
    }
    .genre-next-link::before {
      content: counter(genre-next, decimal-leading-zero);
      font-size: 13px;
      font-variant-numeric: tabular-nums;
      color: var(--faint);
    }
    .genre-next-link strong {
      display: block;
      font-size: 13px;
      font-weight: 600;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      line-height: 1.4;
      color: var(--fg);
    }
    .genre-next-link span {
      display: block;
      color: var(--muted);
      font-size: 14px;
      text-transform: lowercase;
      letter-spacing: 0.022em;
      word-spacing: 0.04em;
      line-height: 1.8;
      transition: color 0.2s;
    }
    .genre-next-link:hover span {
      color: var(--fg);
    }
    @media (max-width: 768px) {
      .genre-next-link {
        grid-template-columns: 40px 1fr;
        gap: 8px 16px;
      }
      .genre-next-link span {
        grid-column: 2;
      }
    }
  </style>

  <section class="hero">
    <div class="hero-background">
      <div class="hero-gradient"></div>
      <div class="hero-noise"></div>
    </div>
    <div class="hero-content">
      <h1 class="headline reveal">{headline}</h1>
      <p class="subhead reveal" data-reveal-delay="0.06s">{subhead}</p>
      <div class="cta">
        <a class="button primary" href={appUrl}>{ctaText}</a>
        <a class="hero-beta-link" href="/">Explore all genres →</a>
      </div>
    </div>
  </section>
  
  <section class="workflow" aria-labelledby="workflow-title">
    <div class="workflow-inner">
      <p class="section-kicker">{kickers.workflow}</p>
      <h2 id="workflow-title" class="workflow-title reveal">{workflowTitle}</h2>
      <div class="workflow-grid">
        {workflowItems.map((item, index) => (
          <div class={`workflow-item ${index % 2 === 1 ? 'workflow-item-reverse' : ''} reveal`} data-reveal-delay={`${0.04 * (index + 1)}s`}>
            <div class="workflow-image">
              <picture>
                <source
                  type="image/avif"
                  srcset={`${item.image}-768.avif 768w, ${item.image}-1280.avif 1280w`}
                  sizes="(max-width: 900px) 100vw, 50vw"
                />
                <source
                  type="image/webp"
                  srcset={`${item.image}-768.webp 768w, ${item.image}-1280.webp 1280w`}
                  sizes="(max-width: 900px) 100vw, 50vw"
                />
                <img src={`${item.image}-1280.webp`} alt={item.imageAlt} loading="lazy" decoding="async" width="1280" height="800" />
              </picture>
            </div>
            <div class="workflow-content">
              <h3 class="workflow-item-title">{item.title}</h3>
              <p class="workflow-item-description" set:html={item.description}></p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  {hasProse && (
    <section class="genre-prose" aria-labelledby="genre-prose-title">
      <div class="genre-prose-inner">
        <p class="section-kicker">{kickers.prose}</p>
        <h2 id="genre-prose-title" class="genre-prose-title">{proseTitle}</h2>
        <div class="genre-prose-body" set:html={proseBody}></div>
      </div>
    </section>
  )}

  {demoTracks.length > 0 && (
    <section class="demo" aria-labelledby="demo-title">
      <div class="demo-inner">
        <div class="demo-header">
          <div class="demo-header-content">
            <p class="section-kicker">{kickers.demo}</p>
            <h2 id="demo-title" class="demo-title">{demoTitle || `Hear ${genre} Beats Made with Sonura`}</h2>
          </div>
        </div>
        <div class="demo-tracks">
          {demoTracks.map((track, index) => (
            <div class="player reveal" data-reveal-delay={`${0.06 * index}s`} role="region" aria-label="Audio player" data-track={index}>
              <div class="player-header">
                <div class="player-art" aria-hidden="true">
                  <div class="art-waveform"></div>
                  <img src="/record.jpeg" alt={`${track.title} artwork`}/>
                </div>
                <div class="player-info">
                  <div class="player-track">{track.title}</div>
                  <div class="player-tags">
                    {track.tags.map((tag) => (
                      <span class="tag tag-genre">{tag}</span>
                    ))}
                  </div>
                  <div class="player-use-case">{track.useCase}</div>
                </div>
              </div>
              <div class="player-main">
                <div class="player-controls">
                  <button class="player-back button icon" aria-label="Back 10 seconds"><i data-lucide="skip-back"></i></button>
                  <button class="player-play button primary circle" aria-label="Play"><i data-lucide="play"></i></button>
                  <button class="player-forward button icon" aria-label="Forward 10 seconds"><i data-lucide="skip-forward"></i></button>
                </div>
                <div class="player-progress">
                  <span class="player-current time">0:00</span>
                  <input class="player-seek" type="range" min="0" max="100" value="0" aria-label="Seek"/>
                  <span class="player-duration time">0:00</span>
                </div>
                <div class="player-side">
                  <div class="volume">
                    <i data-lucide="volume-2" aria-hidden="true"></i>
                    <input class="player-volume" type="range" min="0" max="1" step="0.01" value="0.9" aria-label="Volume"/>
                  </div>
                </div>
              </div>
              <audio class="player-audio" preload="metadata">
                <source src={track.audioSrc} type="audio/mpeg"/>
              </audio>
            </div>
          ))}
        </div>
      </div>
    </section>
  )}

  <section class="genre-next" aria-labelledby="genre-next-title">
    <div class="genre-next-inner">
      <p class="section-kicker">{kickers.next}</p>
      <h2 id="genre-next-title" class="genre-next-title">Finish the {genre} workflow</h2>
      <div class="genre-next-grid">
        <a class="genre-next-link" href="/pricing/">
          <strong>Plans and credits</strong>
          <span>Upgrade for more generations, high-quality downloads, and commercial releases.</span>
        </a>
        <a class="genre-next-link" href="/features/stem-export/">
          <strong>Export stems</strong>
          <span>Pull out drums, bass, melody, vocals, and FX for mixing in your DAW.</span>
        </a>
        <a class="genre-next-link" href="/use-cases/type-beats/">
          <strong>Sell type beats</strong>
          <span>Turn genre pages into beat-selling workflows with commercial rights.</span>
        </a>
        <a class="genre-next-link" href="/plugin/">
          <strong>Use the plugin</strong>
          <span>Generate loops, drums, vocals, and one-shots inside your session.</span>
        </a>
        <a class="genre-next-link" href="/alternatives/suno-alternative/">
          <strong>Compare Suno</strong>
          <span>See why producers use Sonura for faster iteration and stem control.</span>
        </a>
      </div>
    </div>
  </section>

  <section class="faq" id="faq" aria-labelledby="faq-title">
    <div class="faq-inner">
      <p class="section-kicker">{kickers.faq}</p>
      <h2 id="faq-title" class="faq-title">{genre} Beat Maker FAQ</h2>
      <div class="faq-list">
        {faqs.map((faq, index) => (
          <details class="reveal" data-reveal-delay={`${0.03 * index}s`}>
            <summary>{faq.question}</summary>
            <p set:html={faq.answer}></p>
          </details>
        ))}
      </div>
    </div>
  </section>

  <section class="final-cta" aria-labelledby="final-cta-title">
    <div class="final-cta-inner">
      <h2 id="final-cta-title" class="final-cta-title reveal">{finalCtaTitle || `Start Making ${genre} Music Now`}</h2>
      <p class="final-cta-subtitle reveal" data-reveal-delay="0.06s">free to start. no credit card required.</p>
      <div class="final-cta-form reveal" data-reveal-delay="0.12s">
        <a class="button primary large" href={appUrl}>{finalCtaButtonText || `create ${genre.toLowerCase()} music free`}</a>
      </div>
      {trustItems.length > 0 && (
        <div class="final-cta-trust reveal" data-reveal-delay="0.18s">
          {trustItems.map((item) => (
            <div class="trust-item"><i data-lucide="check"></i> <span>{item}</span></div>
          ))}
        </div>
      )}
    </div>
  </section>
</BaseLayout>
