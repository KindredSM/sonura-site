---
import BaseLayout from './BaseLayout.astro';

interface GuideSection {
  title: string;
  body: string;
  bullets?: string[];
}

interface GuideLink {
  title: string;
  href: string;
  description: string;
}

interface Faq {
  question: string;
  answer: string;
}

interface Props {
  title: string;
  description: string;
  keywords: string;
  slug: string;
  headline: string;
  subhead: string;
  format: string;
  bestFor: string;
  steps: string[];
  sections: GuideSection[];
  faqs: Faq[];
  relatedGuides?: GuideLink[];
  ctaText?: string;
}

const {
  title,
  description,
  keywords,
  slug,
  headline,
  subhead,
  format,
  bestFor,
  steps,
  sections,
  faqs,
  relatedGuides = [],
  ctaText = 'Download the plugin',
} = Astro.props;

const canonical = `https://sonurastudio.com/plugin/${slug}/`;

const jsonLd = [
  {
    "@context": "https://schema.org",
    "@type": "TechArticle",
    "headline": headline,
    "description": description,
    "author": { "@type": "Organization", "name": "Sonura" },
    "publisher": {
      "@type": "Organization",
      "name": "Sonura",
      "logo": { "@type": "ImageObject", "url": "https://sonurastudio.com/demo-image.jpeg" }
    },
    "mainEntityOfPage": canonical,
    "image": "https://sonurastudio.com/images/plugin-ui.webp"
  },
  {
    "@context": "https://schema.org",
    "@type": "HowTo",
    "name": headline,
    "description": description,
    "step": steps.map((step, index) => ({
      "@type": "HowToStep",
      "position": index + 1,
      "text": step
    }))
  },
  {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    "itemListElement": [
      { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://sonurastudio.com/" },
      { "@type": "ListItem", "position": 2, "name": "Plugin", "item": "https://sonurastudio.com/plugin/" },
      { "@type": "ListItem", "position": 3, "name": headline, "item": canonical }
    ]
  },
  {
    "@context": "https://schema.org",
    "@type": "FAQPage",
    "mainEntity": faqs.map((faq) => ({
      "@type": "Question",
      "name": faq.question,
      "acceptedAnswer": {
        "@type": "Answer",
        "text": faq.answer
      }
    }))
  }
];
---

<BaseLayout
  title={title}
  description={description}
  keywords={keywords}
  canonical={canonical}
  ogTitle={headline}
  ogDescription={description}
  jsonLd={jsonLd}
>
  <article class="pg-guide">
    <header class="guide-hero">
      <a class="back-link" href="/plugin/">Plugin home</a>
      <p class="guide-kicker">{format}</p>
      <h1>{headline}</h1>
      <p>{subhead}</p>
      <div class="guide-actions">
        <a class="button primary" href="/plugin/">{ctaText}</a>
        <a class="button secondary" href="#setup">View setup steps</a>
      </div>
    </header>

    <section class="guide-summary" aria-label="Plugin guide summary">
      <div>
        <span>Plugin format</span>
        <strong>{format}</strong>
      </div>
      <div>
        <span>Best for</span>
        <strong>{bestFor}</strong>
      </div>
      <div>
        <span>Cost</span>
        <strong>Free download + starter credits</strong>
      </div>
    </section>

    <section class="guide-split" id="setup" aria-labelledby="setup-title">
      <div>
        <p class="guide-kicker">Setup</p>
        <h2 id="setup-title">How to use the plugin here</h2>
        <p>These are the practical first-session steps. Install the plugin, let your DAW scan it, then generate royalty-free audio without leaving the project.</p>
      </div>
      <ol class="step-list">
        {steps.map((step) => <li>{step}</li>)}
      </ol>
    </section>

    <section class="section-stack">
      {sections.map((section) => (
        <section class="guide-section">
          <h2>{section.title}</h2>
          <p>{section.body}</p>
          {section.bullets && (
            <ul>
              {section.bullets.map((bullet) => <li>{bullet}</li>)}
            </ul>
          )}
        </section>
      ))}
    </section>

    <section class="guide-faq" aria-labelledby="guide-faq-title">
      <h2 id="guide-faq-title">FAQ</h2>
      <div>
        {faqs.map((faq) => (
          <details>
            <summary>{faq.question}</summary>
            <p>{faq.answer}</p>
          </details>
        ))}
      </div>
    </section>

    {relatedGuides.length > 0 && (
      <section class="related-guides" aria-labelledby="related-guides-title">
        <h2 id="related-guides-title">Related plugin guides</h2>
        <div>
          {relatedGuides.map((guide) => (
            <a href={guide.href}>
              <strong>{guide.title}</strong>
              <span>{guide.description}</span>
            </a>
          ))}
        </div>
      </section>
    )}
  </article>
</BaseLayout>

<style>
  .pg-guide {
    width: 100%;
    max-width: 1120px;
    margin: 0 auto;
    padding: 96px 24px 128px;
    color: var(--fg);
  }

  .guide-hero {
    max-width: 820px;
    margin-bottom: 48px;
  }

  .back-link,
  .guide-kicker {
    display: inline-flex;
    color: #fff;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0;
    text-transform: uppercase;
    text-decoration: none;
  }

  .back-link {
    color: var(--muted);
    margin-bottom: 24px;
  }

  .back-link:hover {
    color: #fff;
  }

  .guide-hero h1 {
    margin: 10px 0 18px;
    font-size: clamp(42px, 7vw, 76px);
    line-height: 1;
    letter-spacing: 0;
  }

  .guide-hero p,
  .guide-split p,
  .guide-section p,
  .guide-section li,
  .step-list li,
  .guide-faq p {
    color: var(--muted);
    line-height: 1.7;
    font-size: 16px;
  }

  .guide-hero > p {
    font-size: 20px;
    max-width: 720px;
  }

  .guide-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-top: 30px;
  }

  .guide-summary {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 12px;
    margin-bottom: 80px;
  }

  .guide-summary div {
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 18px;
    background: rgba(255, 255, 255, 0.03);
  }

  .guide-summary span {
    display: block;
    color: var(--muted);
    font-size: 13px;
    margin-bottom: 8px;
  }

  .guide-summary strong {
    display: block;
    line-height: 1.35;
  }

  .guide-split {
    display: grid;
    grid-template-columns: minmax(0, 0.85fr) minmax(0, 1.15fr);
    gap: 48px;
    align-items: start;
    margin-bottom: 80px;
  }

  .guide-split h2,
  .guide-section h2,
  .guide-faq h2,
  .related-guides h2 {
    margin: 8px 0 14px;
    font-size: clamp(28px, 4vw, 42px);
    line-height: 1.1;
    letter-spacing: 0;
  }

  .step-list {
    margin: 0;
    padding-left: 24px;
  }

  .step-list li + li {
    margin-top: 12px;
  }

  .section-stack {
    display: grid;
    gap: 34px;
    margin-bottom: 80px;
  }

  .guide-section {
    max-width: 820px;
    border-top: 1px solid var(--border);
    padding-top: 32px;
  }

  .guide-section ul {
    margin: 18px 0 0;
    padding-left: 22px;
  }

  .guide-section li + li {
    margin-top: 8px;
  }

  .guide-faq {
    max-width: 860px;
    margin-bottom: 80px;
  }

  .guide-faq details {
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 0;
    background: rgba(255, 255, 255, 0.02);
  }

  .guide-faq details + details {
    margin-top: 10px;
  }

  .guide-faq summary {
    cursor: pointer;
    padding: 18px 20px;
    font-weight: 700;
  }

  .guide-faq p {
    margin: 0;
    padding: 0 20px 20px;
  }

  .related-guides > div {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 12px;
  }

  .related-guides a {
    display: flex;
    flex-direction: column;
    gap: 8px;
    min-height: 116px;
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 18px;
    color: var(--fg);
    text-decoration: none;
    background: rgba(255, 255, 255, 0.03);
  }

  .related-guides a:hover {
    border-color: var(--border-light);
    background: rgba(255, 255, 255, 0.06);
  }

  .related-guides span {
    color: var(--muted);
    font-size: 14px;
    line-height: 1.5;
  }

  @media (max-width: 860px) {
    .guide-summary,
    .guide-split,
    .related-guides > div {
      grid-template-columns: 1fr;
    }

    .guide-split {
      gap: 24px;
    }
  }

  @media (max-width: 640px) {
    .pg-guide {
      padding: 72px 20px 96px;
    }

    .guide-actions {
      flex-direction: column;
    }

    .guide-actions .button {
      width: 100%;
      justify-content: center;
    }
  }
</style>
